import React from 'react';
export default function PairingScreen({ onPaired }: { onPaired: () => void }) {
  return <div style={{ height: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center', background: 'var(--bg-toolbar)' }}>
    <p>Pairing screen (stub)</p>
  </div>;
}
